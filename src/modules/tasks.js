export class Task {
    constructor(id, name, notes, dueDate, priority, done) {
        this.id = id;
        this.name = name;
        this.notes = notes;
        this.dueDate = dueDate;
        this.priority = priority;
        this.done = done;
    }

}